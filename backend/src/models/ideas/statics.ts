import { Schema } from 'mongoose';

export const applyIdeaStatics = (schema: Schema) => {
  // Placeholder for future model-level query helpers (e.g. findByOwner)
  // Kept for structural parity with the User model.
};